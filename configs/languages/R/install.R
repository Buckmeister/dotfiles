#!/usr/bin/env Rscript

install.packages("languageserver",  clean = TRUE, quiet = TRUE)
install.packages("prettycode",      clean = TRUE, quiet = TRUE)
install.packages("prompt",          clean = TRUE, quiet = TRUE)
install.packages("tidyverse",       clean = TRUE, quiet = TRUE)
